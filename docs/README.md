# Screenshots for the README

Drop a few PNGs here so the main README renders nicely on GitHub. Suggested
shots (you can grab them straight from the running app):

- `dashboard.png` — the sidebar dashboard with the report list and a report open
  (referenced at the top of the main README).
- `report.png` — the 概览 page of a report (cards + 进步趋势 banner + trend charts).
- `analyze.png` — the 分析 / 导入 panel (① 下载 + 分析 / ② 导入已分析 SGF).
- `practice.png` — the 失误集 (blunder gallery) page.

Recommended width ~1400px. Once added, optionally reference them in the README's
Features section, e.g.:

```markdown
<p align="center"><img src="docs/report.png" width="90%"></p>
```
